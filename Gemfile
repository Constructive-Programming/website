source "https://rubygems.org"
gemspec

group :development do
  # Linting harness + markdown linter. Hooks are installed with
  # `bundle exec overcommit --install`; CI runs the same checks via
  # scripts/lint.sh (see .overcommit.yml).
  gem "overcommit", "~> 0.68"
  gem "mdl", "~> 0.13"
end
